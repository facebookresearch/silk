defaults:
  - defaults
  - _self_

pickle_dir_list:
  - _target_: silk.config.paths.join
    _args_: [ASSETS, "datasets", "megadepth", "test", "landscape"]
  - _target_: silk.config.paths.join
    _args_: [ASSETS, "datasets", "megadepth", "test", "portrait"]
